@model IEnumerable<ModelAccessLayer.Designation>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DesignationName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DesignationName)
            </td>
            <td>
                    @Html.ActionLink("Edit", "UpdateDesignation", new { id = item.id }) |
                    @Html.ActionLink("Details", "GetDesignationById", new { id=item.id }) |
                    @Html.ActionLink("Delete", "DeleteDesignation", new { id = item.id })
            </td>
        </tr>
}
    </tbody>
</table>
